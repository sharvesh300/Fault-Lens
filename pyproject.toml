[project]
name = "fault-lens"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
license = "MIT"
requires-python = ">=3.14"
dependencies = [
    "pgmpy>=1.1.2",
    "pytest>=9.1.1",
]
